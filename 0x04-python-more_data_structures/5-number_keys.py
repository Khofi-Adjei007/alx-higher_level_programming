#!/usr/bin/python3
def number_keys(a_dictionary):
    # Get the keys of the dictionary using the keys() method
    keys = a_dictionary.keys()

    # Count the number of keys using the len() function
    num_keys = len(keys)

    return num_keys
